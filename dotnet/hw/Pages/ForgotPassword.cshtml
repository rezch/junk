@page
@model BulletinBoard.Pages.ForgotPasswordModel

<h2>Восстановление пароля</h2>
<p>Введите имя пользователя и email. Новый пароль будет установлен сразу (email не проверяется — тестовый режим).</p>

@if (!string.IsNullOrEmpty(Model.Message))
{
    <div style="color: green">@Model.Message</div>
}
@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div style="color: red">@Model.ErrorMessage</div>
}
<form method="post">
    <p>
        <label>Имя пользователя:<br/>
            <input asp-for="UserName" required />
        </label>
    </p>
    <p>
        <label>Email:<br/>
            <input asp-for="Email" type="email" required />
        </label>
    </p>
    <p>
        <label>Новый пароль:<br/>
            <input asp-for="NewPassword" type="password" required />
        </label>
    </p>
    <p>
        <label>Подтвердите новый пароль:<br/>
            <input asp-for="NewPasswordRepeat" type="password" required />
        </label>
    </p>
    <button type="submit">Сменить пароль</button>
    <a href="/" style="margin-left:10px;">На главную</a>
</form>
